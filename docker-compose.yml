version: '3.8'
services:
  app:
    build:
      context: .
      args:
        # The API key is baked in at build time for the static site
        VITE_CLAUDE_API_KEY: ${VITE_CLAUDE_API_KEY}
    ports:
      - "8080:80"
    restart: unless-stopped
